<Window x:Class="dlgZipProgress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Width="400"
        Background="white"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize" Title="Zip Export"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner" SizeToContent="Height">
    
    <StackPanel VerticalAlignment="Center" Margin="20,12">
        
        <DockPanel Margin="0,0,0,4">
            <TextBlock x:Name="StatusLabel" Text="Processing..." />
            <TextBlock x:Name="blkPercent" Text="0%" HorizontalAlignment="right" Margin="0,3,0,0" />
        </DockPanel>
        
        <ProgressBar x:Name="ProgressBar" Height="15" Maximum="100" Foreground="#0078D4"/>

        <Button x:Name="btnCancel" Margin="0,12,0,0" HorizontalAlignment="center" Content="Cancel" Padding="8,1,8,2" Background="WhiteSmoke"/>

    </StackPanel>
    
</Window>