<Window x:Class="dlgSequences"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomControls"
        mc:Ignorable="d"
        Title="Connected Sequences" Height="550" Width="900" WindowStartupLocation="CenterScreen" ShowInTaskbar="False">

    <Grid  VerticalAlignment="top" TextOptions.TextFormattingMode="Display" Margin="16">
        
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" >
            <TextBlock Text="Connected Sequences" FontWeight="bold" FontSize="16" Margin="0,0,0,12"/>
        </StackPanel>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" VerticalAlignment="Top" Margin="0,0,0,12" HorizontalScrollBarVisibility="Auto">
            <Grid x:Name="pnlConnections" MinWidth="120" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2">

            <Separator Margin="0,2,0,12"/>

            <WrapPanel x:Name="pnlSeqStructures" HorizontalAlignment="Center"/>

        </StackPanel>

    </Grid>
</Window>
