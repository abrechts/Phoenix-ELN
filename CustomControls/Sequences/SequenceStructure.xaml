<UserControl x:Class="SequenceStructure"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="90" >

    <UserControl.Resources>

        <SolidColorBrush x:Key="arrowForegroundCol" Color="Orange"/>

    </UserControl.Resources>
    
    <Grid>

        <StackPanel Orientation="Horizontal">

            <!--Dotted left leading connector arrow -->

            <StackPanel x:Name="arrowLeftDotted" Orientation="Horizontal" Margin="0,10,12,0" d:Visibility="Visible" 
                Visibility="Collapsed">
                <Rectangle Stroke="{DynamicResource arrowForegroundCol}" StrokeDashArray="4 4" Width="50" Height="1" VerticalAlignment="Center"/>
                <TextBlock Text="&#10148;" FontSize="11" Foreground="{DynamicResource arrowForegroundCol}" VerticalAlignment="Center" Margin="-3,-2,0,0"/>
            </StackPanel>

            <!--Reactant structure area-->

            <Border x:Name="pnlReactantStructure" CornerRadius="12" BorderThickness="1">
                <Viewbox x:Name="vboxReactantStructure" MinWidth="70" Margin="2,2,2,2"/>
            </Border>

            <!--Right arrow area-->

            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">

                <!--Standard right arrow-->

                <Border x:Name="pnlArrowRight" BorderThickness="1" CornerRadius="8" Margin="2,10,2,0" BorderBrush="#FF7DDEFF" >

                    <StackPanel x:Name="arrowRight" Cursor="Hand" Margin="2,1,2,1">

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="center" Margin="-10,0,0,-3">
                            <TextBlock x:Name="blkSeedSymbol" Text="➤" Foreground="lightblue" d:Visibility="Visible" Visibility="Collapsed" FontSize="9.5" Margin="0,2,3,0"/>
                            <TextBlock x:Name="blkStepNr" d:Text="Step 2" FontSize="11" FontFamily="Segoe UI" Foreground="lightblue" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="4,0,4,-4" Background="Transparent">
                            <Rectangle Stroke="{DynamicResource arrowForegroundCol}"  Width="50" Height="1" VerticalAlignment="Center"/>
                            <TextBlock x:Name="blkUpperLabel" Text="&#10148;" FontSize="11" Foreground="{DynamicResource arrowForegroundCol}" 
                                VerticalAlignment="Center" Margin="-3,-1,0,0"/>
                        </StackPanel>

                        <TextBlock x:Name="blkExpCount"  d:Text="≤ 98.5%" Margin="-8,0,0,2" FontSize="10.5" HorizontalAlignment="Center" Foreground="WhiteSmoke" />

                    </StackPanel>

                </Border>

            </StackPanel>

            <!--Dotted right trailing connector arrow -->

            <StackPanel x:Name="arrowRightDotted" Orientation="Horizontal" Margin="0,10,12,0" d:Visibility="Visible" 
                   Visibility="Collapsed">
                <Rectangle Stroke="{DynamicResource arrowForegroundCol}" StrokeDashArray="4 4" Width="50" 
                       Height="1" VerticalAlignment="Center"/>
                <TextBlock Text="&#10148;" FontSize="11" Foreground="{DynamicResource arrowForegroundCol}"
                       VerticalAlignment="Center" Margin="-3,-2,0,0"/>
            </StackPanel>

        </StackPanel>

    </Grid>
    
</UserControl>
