<UserControl x:Class="ExpTreeHeader"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:data="clr-namespace:ElnCoreModel;assembly=ElnCoreModel"
        xmlns:local="clr-namespace:CustomControls"
        mc:Ignorable="d" 
        d:DataContext="{d:DesignInstance Type=data:tblExperiments}"
        SnapsToDevicePixels="True" x:Name="expTreeHeaderRoot">

    <UserControl.Resources>

        <local:UserTagConverter x:Key="userTagConv"/>
        <local:YieldStrConverter x:Key="yieldStrConv"/>
        <local:WeightFromGramsConverter x:Key="weightFromGramsConv"/>
        <local:IntegerToVisibilityConverter x:Key="intToVisConv"/>
        <local:ExpStateToVisibilityConverter x:Key="expStateVisConv"/>
        <local:PinnedToVisibilityConverter x:Key="pinnedToVisConv"/>

        <Style TargetType="MenuItem">
            <Setter Property="Template" Value="{StaticResource ContextMenuItem}"/>
        </Style>
        
    </UserControl.Resources>

    <UserControl.ContextMenu>
        <ContextMenu>
            <!--<MenuItem x:Name="mnuBookmark" Icon="{StaticResource icoArrowRight}" Header="Bookmark"/>-->     
            <!--<Separator Visibility="{Binding WorkflowState, Converter={StaticResource expStateVisConv}}"/>-->
            <Separator>
                <Separator.Template>
                    <ControlTemplate TargetType="Separator">
                        <TextBlock Text="{Binding ExperimentID}" Background="DimGray" Foreground="white" Padding="6,2,6,3"/>
                    </ControlTemplate>
                </Separator.Template>
            </Separator>

            <MenuItem x:Name="mnuFinalize" Icon="{StaticResource icoOkBullet}" Header="Finalize Experiment" Opacity="0.8"
                Visibility="{Binding WorkflowState, Converter={StaticResource expStateVisConv},ConverterParameter=invert}"/>
            <MenuItem x:Name="mnuUnlock" Icon="{StaticResource icoReopened}" Header="Unlock Experiment" Opacity="0.8"
               Visibility="{Binding WorkflowState, Converter={StaticResource expStateVisConv}}"/>
            
        </ContextMenu>
    </UserControl.ContextMenu>

    <Grid>

        <StackPanel Orientation="Horizontal" Margin="0,0,4,0">

            <TextBlock x:Name="icoTabOpen" Text="📌" FontSize="9" FontWeight="normal" VerticalAlignment="Center" Margin="0,0,0,0"
               Visibility="{Binding DisplayIndex, Converter={StaticResource pinnedToVisConv}}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{DynamicResource PinGlyphForeground}"/>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Border CornerRadius="6" Padding="2,0,2,0"  >
                
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource TreeItemBackground}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCurrent}" Value="1">
                                <Setter Property="Background" Value="{DynamicResource CurrentExperimentHighlight}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <StackPanel Orientation="Horizontal">
               
                    <Grid Width="14" Margin="-1,1,0,0" VerticalAlignment="Center">
                        
                        <Ellipse HorizontalAlignment="Center" VerticalAlignment="Center">
                            
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource WorkflowInProgressFill}"/>
                                    <Setter Property="Width" Value="8"/>
                                    <Setter Property="Height" Value="8"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WorkflowState}" Value="1">
                                            <Setter Property="Fill" Value="{DynamicResource WorkflowFinalizedFill}"/>
                                            <Setter Property="Width" Value="10"/>
                                            <Setter Property="Height" Value="10"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding WorkflowState}" Value="2">
                                            <Setter Property="Width" Value="10"/>
                                            <Setter Property="Height" Value="10"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="8" FontWeight="bold" Margin="0,1,1,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value=""/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WorkflowState}" Value="1">
                                            <Setter Property="Text" Value="✓"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding WorkflowState}" Value="2">
                                            <Setter Property="Text" Value="✶"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                    </Grid>

                    <TextBlock MinWidth="50" Margin="0,0,2,1" VerticalAlignment="Center"
                        Foreground="{DynamicResource BodyForeground}">
                        <Run Name="runExpID" d:Text="demo-00123" Text="{Binding ExperimentID}"
                             Foreground="{DynamicResource ExperimentIdForeground}"/>
                        <Run Text="-"/>
                        <Run Name="runYield" d:Text="92.5%" Text="{Binding Yield, Converter={StaticResource yieldStrConv}}"/>
                        <Run Name="runScale" d:Text="(23.5 g)" Text="{Binding RefReactantGrams, Converter={StaticResource weightFromGramsConv}}"/>
                    </TextBlock>

                </StackPanel>

            </Border>

        </StackPanel>
        
    </Grid>
    
</UserControl>
